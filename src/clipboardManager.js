class ClipboardManager {
    static copy(text) {
        // Implementasi untuk menyalin teks ke clipboard
    }

    static paste() {
        // Implementasi untuk mengambil teks dari clipboard
    }
}

module.exports = ClipboardManager;
