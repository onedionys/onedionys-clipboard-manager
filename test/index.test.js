const assert = require('assert');
const ClipboardManager = require('../src/clipboardManager');

describe('ClipboardManager', () => {
    describe('#copy()', () => {
        it('should copy text to clipboard', () => {
            // Write a test here
            // Contoh: assert.equal(ClipboardManager.copy('test'), true);
        });
    });

    describe('#paste()', () => {
        it('should return text from clipboard', () => {
            // Write a test here
            // Contoh: assert.equal(ClipboardManager.paste(), 'test');
        });
    });
});
